
node_modules/
dist/
dist-test/
*.log
.env
.env.local
.DS_Store

# 工具运行时目录（不应进入公开仓库）
.reasonix/
.impeccable/

# Rust 部署器构建产物（由 GitHub Actions / 本地交叉编译产生）
deployer/target/
# 离线镜像包（大体积，走 Release 附件，不进 git）
images.tar
